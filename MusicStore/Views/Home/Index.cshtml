@* @model HomeViewModel

@{
    ViewData["Title"] = "Home Page";
}


<div class="row">
    @foreach (var instrumentos in Model.InstrumentosDestaque)
    {
        <partial name="_InstrumentosResumo" for="@instrumentos" />
    }
</div> *@

@model HomeViewModel

@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
    <h2 class="text-center mb-5">Destaques do Mês</h2>
    <hr class="mb-4">
    <div class="row">
        @foreach (var instrumento in Model.InstrumentosDestaque)
        {
            <partial name="_InstrumentosResumo" for="@instrumento" />
        }

    </div>
</div>
